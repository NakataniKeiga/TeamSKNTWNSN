using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class MocScene : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //SceneManager.LoadScene("MocStage1",LoadSceneMode.Additive);
        //SceneManager.LoadScene("UIScene", LoadSceneMode.Additive);

    }

    // Update is called once per frame
    void Update()
    {
        //if (Input.GetKeyDown(KeyCode.Alpha1))
        //{

        //    SceneManager.LoadScene("MocStage1", LoadSceneMode.Additive);
        //    var currntScene = SceneManager.GetActiveScene();
        //    Debug.Log(currntScene);
        //    SceneManager.UnloadSceneAsync("MocStage2", UnloadSceneOptions.None);
        //}

    }
}
